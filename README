# jonesrussell's home directory setup

## Install

```
git clone https://github.com/jonesrussell/home-directory.git ~/.home-directory
cd ~/.home-directory
./install.sh
```

## Update

```
cd ~/.home-directory
git pull
./install.sh
```

